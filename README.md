# lands-capture-waypoints
A Fabric mod that creates a JourneyMap waypoint when a Lands plugin capture block is placed during a war.


Designed for the server Stoneworks: play.stoneworks.gg

Releasing this on GitHub so you don't have to trust that the file isn't malware.


Dependencies:

Fabric API,JourneyMap
